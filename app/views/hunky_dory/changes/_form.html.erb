<%= form_with(model: change, local: true) do |form| %>
  <% if change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(change.errors.count, "error") %> prohibited this change from being saved:</h2>

      <ul>
      <% change.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :summary %>
    <%= form.text_field :summary, id: :change_summary %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text, id: :change_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
